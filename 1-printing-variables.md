## std::cout, float, double, int

# 

- `std::cout` wyświetla tekst to okna cmd.
przykład: `std::cout << "Wiadomość\n"`
- `\n` jako tekst oznacza znak `Enter` (nowa linia)
- tekst musi zaczynać i kończyć się znakiem `"`
- std::cout
